##  before the real dataset: A. we use this agent to generate report from image
##  after the real dataset: B. we use this agent to evaluate the report and return some metrics

# before the real dataset, pathology agent has two abilities: A + B
# after the real dataset, pathology agent no longer needs A, so we simple remove A from A+B